# v10 — Honest correction: the Holonomy Code is NOT a generativity mechanism (and what genuinely survives)

**This document retracts a claim.** `v9.md` presented the rich Holonomy Code as "the strongest positive
artifact of the program," with a measured generativity `Δ = D̄ − log₂K = +1244` bits. A dedicated
deflationary audit — run as an independent Opus agent that **executed the code** — showed that claim is
**fatally flawed by the same measure-mismatch that `v5.md` §5.1 already conceded**. I am recording the
retraction, the deeper clarification it forces, and the results that genuinely survive (some of them
strong). The adversarial process worked exactly as it is supposed to: it caught an over-claim of mine.
`v9.md` should be read only through this correction.

## 1. The retraction (audit findings, all verified by running the code)

- **`Δ = +1244` is fake (FATAL).** The "rich field" `D̄ = 1246` bits is quenched-disorder noise. The
  disorder rule `R` is a PRNG stream from `default_rng(0)` — its true Kolmogorov complexity is `O(1)`
  (one seed + ~30 lines). Charged operationally (zlib of `R`) and amortized over its *actual* `K=4`
  targets, `Δ = D̄ − (zlib(R)/K + log₂K) = 1246 − (6472/4 + 2) = −374` bits — **negative**. `D̄` measured
  an incompressibility the generator does not possess. This is v5's relocation, re-hidden behind
  "amortization" of a substrate charged nowhere.
- **The winding class carries ~1.3% of the field (FATAL).** ANOVA `η²` across basins/classes: the
  winding label explains **1.3%** of realized-field variance; 98.7% is basin + disorder. Within-class
  correlation (same winding, different seed) is ~0.001 — as low as cross-class ~0.06. The low
  cross-class correlation is **glassy decorrelation**, not class-carried distinctness. "K distinct rich
  targets" is hollow.
- **Richness (eff-dim 6.6) is the disorder's (FATAL).** Raw disorder field eff-dim = 6.35; clean plane
  wave = 1.0; realized = 6.6. The richness is the fixed disorder pattern showing through, not a
  generated/specified target — combined with `η²=1.3%`, the class contributes ~nothing to it.
- **Self-repair was cherry-picked to the trivial `(0,0)` sync class (FATAL).** It owns 317/400 basins
  (robust for trivial reasons). Re-seeded on nonzero classes, most do not lock; the one that does shows
  non-monotone slip noise. No repair radius for any nontrivial target.
- **Partial:** `K=4` is tiny and basin-threshold-unstable (4 or 6); "no energy functional" is correct
  physics but decorative for a generativity claim (nothing rich was placed); and stripped of vocabulary
  the construction is **twisted-state multistability in a Kuramoto lattice** (Wiley–Strogatz–Girvan),
  with `w=(0,0)` = the sync/consensus (Family-A) state.

**Strongest surviving form of v9's construction:** *a fixed disordered non-reciprocal Kuramoto lattice
is multistable over a small set of winding-number twisted states, one being the trivial sync state; the
winding number is a genuinely non-local topological label; the trivial class is robust to ~30% phase
randomization; the dynamics is a true NESS (persistent current, no global Lyapunov).* Correct, modest,
known. Everything past it — rich, distinct, generativity `+1244`, codebook, self-repair-as-law — does
not survive.

## 2. The deeper clarification: genuine vs apparent generativity

The audit forces the sharpest statement the program has reached about what generativity **is**.

The Holonomy Code's "harmonic completion" — the drive computing the full phase field from an integer
winding class — was meant to be the generativity engine. It is not, and the reason is general:

- **Clean (no disorder):** the harmonic representative of a winding class on a torus is a *plane wave* —
  fully determined by the `log₂K` class bits (plus `O(log L)`). So `D̄_genuine ≈ log₂K` and `Δ ≈ 0`.
  This is **Claim A** (the selection specifies the target); no generativity.
- **Disordered:** the field acquires high `D̄` (zlib), but that content is **uncharged PRNG noise**, not
  class-carried (`η² ≈ 1%`). So `Δ` is large but **fake**.

The `K`–richness–`η²` tradeoff, measured directly (`experiments/v11_holonomy_scaling.py`,
`v11_results.log`):

| disorder | K (codebook) | richness (spectral PR) | `η²` (class-carried) | regime |
|---|---|---|---|---|
| 0.10 | **48** | 2.3 | **1.00** | large distinct codebook, but each field is a plane wave the class fully determines → `D̄ ≈ log₂K` → **Δ ≈ 0 (Claim A)** |
| 0.25 | 25 | 2.6 | 1.00 | " |
| 0.50 | 10 | 3.4 | 1.00 | " |
| 0.90 | 1 | 15.8 | **0.00** | rich field, but the richness is **disorder** (`η²→0`), uncharged PRNG noise → **fake Δ** |
| 1.60 | 1 | 50.9 | 0.00 | " |

The two failure modes are exactly the ones §2 predicts. **Low disorder:** a genuinely large codebook
(`K=48`) of distinct (`η²=1`), non-local, self-repairing classes — but each is a low-rank plane wave the
class *fully specifies* (`η²=1` ⇒ `D̄ ≈ log₂K` ⇒ `Δ≈0`), i.e. **Claim A** with no generativity. **High
disorder:** rich fields (`richness` up to 51) but `η²→0` — the richness is disorder, not class, so
**Δ is fake**. There is no regime with high richness AND high `η²` AND `K>1`.

There is **no regime with both high richness and high `η²`**: fixed physics adds either nothing genuine
(plane wave, Claim A) or ungenerated noise (disorder). **Genuine generativity requires the shared
substrate to be a *learned prior* — one that adds target-relevant structure the per-target selection did
not specify — not fixed physics and not uncharged disorder.**

This is exactly what distinguishes the program's *surviving* positive result:

- **The NCA codebook (v6–v8) PASSES this test.** Its shared body is a *learned* prior; a small per-target
  code selects a **held-out** target realized at corr ~0.78 — i.e. the code carries the target's
  identity (high class-explanatory-power / held-out realizability, B1's necessary+sufficient condition).
- **The Holonomy Code FAILS it** (`η² ≈ 1%`; the winding carries almost none of the field).

So the audit does not just delete a result — it **validates held-out realizability / class-explanatory
power as *the* discriminator** between genuine and fake generativity, and re-confirms that the program's
genuine generativity lives in the NCA codebook, not the Holonomy Code.

## 3. What genuinely survives from the Holonomy work (and it is valuable)

The Holonomy Code fails as a *generativity* mechanism but succeeds as a *non-local, deception-resistant
encoding* — a contribution on the one hazard the program had never touched (H3), plus a correct theory.

### 3.1 Type-B resistance = code distance (executed, `experiments/v11_typeB_holonomy.py`)

A loop-integral (winding) readout is **structurally unfakeable by self-report**, unlike a self-reported
scalar. Measured on the non-reciprocal ring (`N=24`):

- **Self-report scalar (the H3 baseline):** one Type-B liar shifts the aggregate by `2/N = 0.083` with
  **0 flags** — the deceptive-misreport failure no aggregation architecture in the program defends.
- **Loop integral:** a single liar's contribution **telescopes** (it enters two incident edge-differences
  with opposite sign) → the winding is unchanged; any actual class flip forces an edge to `≈π` (a
  phase-slip **defect**), converting the Type-B lie into a **visible Type-A** event a monitor catches.
- **Minimum collusion for an undetected clean class flip = the systole = the code distance**: measured
  `min|S| = 7 → 12 → 18 → 21 → 23 → 24` as the monitor tightens (→ full systole `N=24`).
- **True dynamics:** a divergent-true-but-clean-report state is physically impossible — the coupling
  either heals the liar (1–2 units → back to class `w`) or genuinely slips the class (4,12 units → a new
  *clean* class); the settled field is always a clean codeword, never a persistent hidden divergence.

**The unification:** the single parameter `Δ = systole(G)` governs self-repair radius (B3b), edit cost
(B3c/H4), **and** the minimum-collusion Type-B threshold — one number for robustness, correction, and
deception cost. Structurally this is the **missing-self / MHC** analog (Ljunggren–Kärre 1990): the signal
is defined by what neighbours *jointly present* (edge differences), not by what a unit *says about
itself*, so a lie surfaces as a *missing/anomalous relational feature* (the defect), not a trusted false
claim. Residual exposure is exactly the code distance (a ≥-systole coordinated cut installs a genuine
other codeword — the irreducible error-correction bound) and the integrity of the readout/observer.

### 3.2 The holographic threshold theorem (correct theory)

The winding pair `(w_x,w_y)` are **literally the logical operators of Kitaev's toric code** (`H₁(T²)`).
This makes B3's worst-case parameter `Δ` a two-regime **threshold**: worst-case distance `d = systole`
(non-localization = self-repair = edit-cost, the H4 identity), and a *typical* i.i.d.-erasure threshold
`p_c = 1/2` (square-lattice bond percolation, by primal/dual self-duality; Delfosse–Zémor,
Stace–Barrett), constant even as `d/N → 0`. `log₂K = β₁·log₂|Σ|` (genus-1: `β₁=2` → `log₂K=2`, matching
the measured `2.00` exactly). The classical phase field realizes the one-basis (classical `ℤ₂`
homological) sector; a logical *qubit* (conjugate-pair protection) would need quantum coherence and is
neither used nor claimed. This is a correct, quantitative sharpening — but note it is a theory of the
**code/label** (non-localization + self-repair thresholds), *not* of generativity; per §2 the target the
code carries is `log₂K = β₁` bits, so where the class is rich (large `β₁`) `log₂K` is correspondingly
large and `Δ = D̄ − log₂K` does not inflate.

### 3.3 A DL-free generativity measure (the localization spectrum) — and the reconciliation

An interventional agent built a **description-length-free** replacement for `Δ`
(`experiments/v11_localization_spectrum.py`, executed): perturb each causal channel (shared **rule**,
**coupling**, runtime **state**, **drive**), re-relax to the attractor, and measure the response. Its
readings: `dim_rule` = effective dimension of attractor-change under rule/coupling perturbations;
`dim_sel` = effective dimension the per-target **selection** commands; `G = dim_rule/dim_sel`; healing
`H` = does a kicked attractor restore the target; plus a **Levin** coupling-leverage (a transient
coupling edit, genome fixed, that deposits a persistent new class — the two-headed-planarian signature).
Measured: B-bio(Kuramoto) `G=21.1, H=0.88`, Claim-A(stored vector) `G=0, H=0`, Trivial gated out. It is
**immune to the zlib over-count that voided `Δ`**: a low-rank target moves in a low-dimensional manifold,
so `dim_rule` sees the true rank, not the compressor's inflation.

**The reconciliation with the audit (important).** Taken at face value, `G=21` for the Holonomy Code
looks like "B-bio confirmed" — in tension with `η²=1.3%`. The resolution: `G = 21.1 / dim_sel` with
`dim_sel ≈ 1.0`. **The high `G` is driven by a *trivial* `dim_sel`** — the selection commands ~1
dimension of morphology, which is *exactly* the audit's `η²≈1%` finding restated causally. High `G` via
tiny `dim_sel` means "rich substrate, **trivial** selection," not distinct-target generativity. So the
localization spectrum, read via `dim_sel` (not `G` alone), **confirms** the audit rather than
contradicting it. The DL-free generativity discriminator is `dim_sel` / `η²` (does the selection command
*distinct* target identities) — **not** `G` and **not** healing, as the NCA run below shows by refuting
both. Run on the NCA codebook (`experiments/v11_nca_localization.py`):

`dim_sel = 4.3` (18 targets span 4.3 effective dims — **distinct** targets) vs the Holonomy Code's
`dim_sel ≈ 1.0` and Claim-A's `dim_rule ≈ 0`. So `dim_sel` cleanly separates the three.

**But this run also refuted the composite `G`/`H` reading, honestly:** the NCA measures `G=0.91, H=0.15`
while the Holonomy Code measures `G=21, H=0.88` — i.e. `G` and healing flag the *Holonomy Code* as
*more* "B-bio" than the genuine NCA codebook, which is **wrong**. `G` is inflated precisely by the
Holonomy Code's trivial `dim_sel≈1`, and healing measures *self-repair* (topological protection), not
generativity (the NCA body here was trained without damage, so `H` is low though its generativity —
held-out realizability 0.78 — is genuine). **Conclusion: of the localization spectrum, only `dim_sel` is
a robust generativity discriminator; `G` and `H` are not** (they are gameable by trivial selection and
by self-repair respectively). And `dim_sel` is the *same* quantity as the audit's `η²` and B1's held-out
realizability — "does the per-target selection command distinct target identities." Held-out
realizability (B1) remains the cleanest, least-gameable form.

This is the DL-free discriminator the program needed, and it unifies three threads: the interventional
`dim_sel`, the audit's `η²`, and B1's held-out realizability are **the same quantity** — does the
per-target selection carry the target's identity? The NCA codebook passes; the Holonomy Code fails.

## 4. Corrected standing of the whole program

- **Genuine positive generativity: the NCA codebook (v6–v8).** A learned shared prior + small per-target
  code realizes held-out targets (backprop-certified 0.78; low-`I`-buildable given capacity, v8). This
  is the program's real constructive result, and the audit *strengthens* it by contrast (it passes the
  discriminator the Holonomy Code fails). Its honest limits stand (modest fidelity, toy scale).
- **Impossibility side: O2 + B1** (selection cost, not specification cost; achievable iff target-agnostic
  codebook / held-out realizability). Unaffected, and now the discriminator they define is empirically
  validated as load-bearing.
- **Holonomy Code: NOT generativity.** It is (i) a genuinely non-local, **Type-B-resistant encoding**
  (`Δ`=systole, executed) — a real result on H3; and (ii) an instance of a **toric-code / homological
  threshold theorem** (correct theory). Its generativity claim is retracted.
- **The meta-lesson, made concrete:** "rich field" measured by a generic compressor is not target
  richness; **class-explanatory-power / held-out realizability is the only measure that separates
  generated target-structure from ungenerated noise**, and it must gate every generativity claim. Two of
  the program's own constructions (v5 NCA at low rank; v9 Holonomy at high disorder) failed it in
  opposite directions before it was enforced.

## 5. What remains genuinely open

- **Scale the NCA codebook** (the surviving positive) with statistics — the honest verification of v6–v8
  at larger grids, more seeds, richer *learned* targets (Prong B / v12), so the one real generativity
  result is on firm quantitative ground rather than toy single-seed points.
- **Type-B on high-`β₁` graphs**: does `min-collusion = systole` decouple codebook size from deception
  cost as the graph's systole spectrum predicts?
- **An interventional / causal measure** of "where the target lives" that sidesteps description length
  entirely — the alternative operationalization that would have caught the v9 over-claim earlier
  (Prong E).
